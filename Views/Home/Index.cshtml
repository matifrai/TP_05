@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    
    <div class="mt-4">
        <a href="@Url.Action("Login", "Home")" class="btn btn-primary btn-lg me-3">
            Iniciar Sesión
        </a>
        <a href="@Url.Action("Registrarse", "Home")" class="btn btn-success btn-lg">
            Registrarse
        </a>
    </div>
</div>
